---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dvls_entry_folder Resource - terraform-provider-dvls"
subcategory: ""
description: |-
  A DVLS Folder Entry. Folders organize other entries inside a vault. Credential entries that set folder require the folder to exist beforehand.
---

# dvls_entry_folder (Resource)

A DVLS Folder Entry. Folders organize other entries inside a vault. Credential entries that set `folder` require the folder to exist beforehand.

## Example Usage

```terraform
resource "dvls_entry_folder" "parent" {
  vault_id    = "00000000-0000-0000-0000-000000000000"
  name        = "foo"
  description = "parent folder at the vault root"
}

resource "dvls_entry_folder" "child" {
  vault_id      = "00000000-0000-0000-0000-000000000000"
  name          = "bar"
  parent_folder = dvls_entry_folder.parent.name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the folder. Must not contain a backslash.
- `vault_id` (String) The ID of the vault.

### Optional

- `description` (String) The description of the folder.
- `parent_folder` (String) Path of the parent folder. Omit for a folder at the vault root.

Changing this forces replacement: DVLS has no folder-move API. Any entries still in the folder when it is destroyed are **orphaned** (kept in the vault but unreachable by a `folder = ...` reference). Move entries out before changing `parent_folder`.

### Read-Only

- `id` (String) The ID of the folder. This is set by the provider after creation.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# This resource can be imported using `<vault_id>/<entry_id>` format, e.g.
terraform import dvls_entry_folder.example 00000000-0000-0000-0000-000000000000/00000000-0000-0000-0000-000000000000
```
